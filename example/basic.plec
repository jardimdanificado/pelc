require std;
help;
set x 20;
set y 7;
set z !(solve &x/10)!;
echo &z;
echo today is day !(solve &x + &y)! of month !(solve 3 * &z)!;
-- note that even in comments you can run code !($ ls)!;
def abc
    return 'example text showing a just defined command!';
set a !(abc)!;
echo &a;
exit; -- quits program